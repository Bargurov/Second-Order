# Public Repo Cleanup Plan

Purpose: classify the remaining untracked/quarantine artifacts before any public-facing cleanup. No files were deleted while preparing this plan.

## Tracked Artifact Cleanup Priorities

This section records the 2026-05-06 tracked public-surface audit. It is a plan
only: do not edit `.gitignore`, remove files, or move files until the cleanup
slice is explicitly started.

| Priority | Path / surface | Action | Why | Verification command |
|---|---|---|---|---|
| 1 | `tests/test_export_*.db` | `git rm --cached` | 783 generated SQLite export DBs are tracked (~20.5 MB). They are already covered by `.gitignore`, but tracked files stay public until explicitly untracked. | `git ls-files "tests/test_export_*.db"` should return nothing after cleanup. |
| 2 | `frontend/tsconfig.app.tsbuildinfo`, `frontend/tsconfig.node.tsbuildinfo` | `git rm --cached` | TypeScript incremental build caches are generated and already ignored by `*.tsbuildinfo` rules. | `git ls-files "*.tsbuildinfo"` should return nothing after cleanup. |
| 3 | `baseline_output.json` | `git rm --cached` | Generated by `scripts/magic_number_baseline.py` and currently contains a local absolute path. Keep regenerated output local unless a scrubbed fixture is intentionally designed. | `git ls-files baseline_output.json` should return nothing; `git grep -n "C:/Users\|C:\\Users" -- "*.json" "*.md"` should not find public path leaks. |
| 4 | `news_inbox.json` | Review later | It is referenced by README and `news_fetch.py` as the local inbox. Decide whether it is a public seed fixture or a local-only operator file before changing tracking. | `git grep -n "news_inbox.json"` and inspect whether tests/docs require a committed seed file. |
| 4 | `scripts/headline_snapshot.json` | Review later | Used by threshold/topic-balance tooling, but it is snapshot data rather than code. Decide whether to keep as a named fixture, regenerate locally, or move under a fixtures/archive path. | `git grep -n "headline_snapshot.json"` and rerun the associated validation script before any move. |
| 5 | `docs/superpowers/**` | Move to `docs/archive` or review later | Some specs contain durable product decisions, but plans include local agent workflow noise. Public docs should not read like raw task logs. | `git ls-files "docs/superpowers/**"` plus `git grep -n "superpowers:" docs/superpowers`. |
| 5 | `ARCH_AUDIT.md` | Move to `docs/archive` | Useful historical audit, but root-level stale critical findings can misrepresent current health. | `git grep -n "ARCH_AUDIT.md"` before moving references. |
| 5 | `DESIGN.md` | Review later / move to `docs/archive` | Current approved design source is `design/extracted/screens/`; root `DESIGN.md` may be stale, but frontend comments and `CLAUDE.md` still reference it. | `git grep -n "DESIGN.md"` before moving references. |
| 5 | `EVALUATION.md` | Keep tracked | Referenced by tests and validation modules; it documents current eval/threshold discipline. | `git grep -n "EVALUATION.md"` should continue to resolve after cleanup. |
| 5 | `cv_framing.md` | `git rm --cached` or move private | Personal portfolio/CV framing is not product source and weakens public repo focus. | `git ls-files cv_framing.md` should return nothing after cleanup if untracked. |
| 6 | `app.py` | Move to `docs/archive` or review later | It self-labels as frozen Streamlit legacy and AGENTS says not to treat it as maintained product. Root placement is confusing beside FastAPI/React. | `git grep -n "app.py"` before moving; verify no maintained route/test imports depend on it. |

## Final Untracked-Candidate Decisions

The earlier untracked-candidate triage remains below. These are the final recommended actions for the remaining candidates; no files were deleted, staged, or committed while preparing this table. On 2026-05-07, the only new ignore rules added were for generated design exports.

| Path | Final action | Reason |
|---|---|---|
| `.githooks/pre-commit` | Commit selected files | Single useful hook candidate, but only commit after documenting opt-in hook setup so contributors are not surprised. |
| `design/extracted/screens/*.jsx`, `design/extracted/styles.css`, `design/extracted/primitives.jsx`, `design/extracted/data.jsx` | Commit selected files | Source-like design references aligned with the approved design-source role. Review path mismatches first because approved docs name some files under `screens/` while the export has shared files at `design/extracted/`. |
| `design/extracted/movers.jsx`, `design/extracted/movers.css`, `design/extracted/movers_data.jsx`, `design/extracted/app.jsx` | Move/archive later | Potentially useful design-source candidates, but not all are in the approved source list. Keep visible until a design-source review either selects them or archives them. |
| `design/second-order-design.zip`, `design/extracted/Second Order.html`, `design/extracted/Second Order-print.html` | Ignore local-only | Generated/export artifacts are public-repo noise and now have precise `.gitignore` rules. |
| `docs/superpowers/specs/*.md` | Move/archive later | Some specs contain useful product decisions, but they should be checked for stale V1 wording and normalized into public docs or `docs/archive` before adding more. |
| `docs/superpowers/plans/*.md` | Delete later after extracting decisions | Mostly local workflow/task planning noise. Extract durable decisions into normal docs, then delete or keep private locally. Do not add broad ignore rules while tracked `docs/superpowers/**` files still exist. |
| `scripts/rebuild_archive.py` | Commit later after review | Prior triage found this valuable and that still applies. It defaults to dry-run, but has `--write`; pair it with tests, backup guidance, and runbook warnings before public commit. |
| `tests/test_events_archive_detail_consistency.py` | Commit now | Prior triage found this valuable and that still applies. It is a zero-cost archive read-surface regression test and currently passes in the events discovery suite. |

## Credibility Risks

- Local agent state (`.superpowers/`, `docs/superpowers/**`) can make the repo look like a scratchpad rather than a maintained product. `.superpowers/` is now ignored; `docs/superpowers/**` remains unignored for a future content decision.
- Generated design/export artifacts (`design/*.zip`, exported HTML, root extracted files) blur the line between approved design source and stale generated output.
- Generated eval/report files without context (`eval_run_index.json`, raw calibration/topic reports) can look like unsupported performance claims.
- Operational scripts that can write to the archive (`scripts/rebuild_archive.py`) should not appear without tests, backup guidance, and clear dry-run defaults.
- Empty or duplicate templates (`VALIDATION_LOG.md`) add noise and weaken public documentation focus.

## Design Asset Triage

`design/` currently contains 15 files, about 234 KB total:

- 10 `.jsx` source/reference files
- 2 `.css` files
- 2 exported `.html` files
- 1 `.zip` archive

The README currently points broadly to `repo/design/`, which is directionally correct but too broad for public inclusion. The source-like files under `design/extracted/` match the approved design-reference role. The generated HTML exports and `design/second-order-design.zip` should not be tracked in the public repo.

Recommendation: keep and commit selected design source files only. Do not commit the zip archive or exported HTML. A later design-source commit should either narrow the README reference to the selected committed paths or add a short note explaining that generated exports are intentionally excluded.

## Staged Cleanup Recommendation

1. Done: ignore local-only generated/quarantine state: `.superpowers/`, `gimp/`, root report logs, and `eval_run_index.json`.
2. Next quick commit candidate: `tests/test_events_archive_detail_consistency.py`.
3. Review-before-commit candidates: `.githooks/pre-commit`, selected `design/extracted/` source files, useful `docs/superpowers/specs/*.md`, and `scripts/rebuild_archive.py`.
4. Done: ignore generated design exports: `design/second-order-design.zip` and exported HTML.
5. Quarantine local workflow docs: `docs/superpowers/plans/*.md` after extracting any durable decisions.
